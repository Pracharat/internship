import { StrictcharacterDirective } from './strictcharacter.directive';

describe('StrictcharacterDirective', () => {
  it('should create an instance', () => {
    const directive = new StrictcharacterDirective();
    expect(directive).toBeTruthy();
  });
});
