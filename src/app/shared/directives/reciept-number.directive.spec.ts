import { RecieptNumberDirective } from './reciept-number.directive';

describe('RecieptNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new RecieptNumberDirective();
    expect(directive).toBeTruthy();
  });
});
