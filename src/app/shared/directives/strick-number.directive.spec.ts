import { StrickNumberDirective } from './strick-number.directive';

describe('StrickNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new StrickNumberDirective();
    expect(directive).toBeTruthy();
  });
});
