import { RemarkDirective } from './remark.directive';

describe('RemarkDirective', () => {
  it('should create an instance', () => {
    const directive = new RemarkDirective();
    expect(directive).toBeTruthy();
  });
});
