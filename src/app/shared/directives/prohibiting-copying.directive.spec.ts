import { ProhibitingCopyingDirective } from './prohibiting-copying.directive';

describe('ProhibitingCopyingDirective', () => {
  it('should create an instance', () => {
    const directive = new ProhibitingCopyingDirective();
    expect(directive).toBeTruthy();
  });
});
