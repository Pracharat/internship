import { PreventDefaultClickDirective } from './prevent-default-click.directive';

describe('PreventDefaultClickDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventDefaultClickDirective();
    expect(directive).toBeTruthy();
  });
});
