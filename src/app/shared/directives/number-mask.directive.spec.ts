import { NumberMaskDirective } from './number-mask.directive';

describe('PhoneMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberMaskDirective();
    expect(directive).toBeTruthy();
  });
});
