import { ClickScrollTopLeftDirective } from './click-scroll-top-left.directive';

describe('ClickScrollTopLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickScrollTopLeftDirective();
    expect(directive).toBeTruthy();
  });
});
